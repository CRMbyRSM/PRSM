// Certificate error modal removed — bridge transport uses plain HTTP, no TLS negotiation needed.
// This stub is kept so App.tsx import doesn't break.

export function CertErrorModal() {
  return null
}
